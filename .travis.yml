branches:
    only:
        - master

dist: trusty

install:
    - composer install

language: php

matrix:

    allow_failures:
        - php: nightly
        - php: hhvm-nightly

    fast_finish: true

php:
    - 5.6
    - 7.0
    - hhvm-3.12
    - nightly
    - hhvm-nightly

sudo: required
