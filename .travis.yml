branches:
    only:
        - master

install:
    - composer install

language: php

matrix:

    allow_failures:
        - php: nightly
        - php: hhvm

    fast_finish: true

php:
    - 7.0
    - nightly
    - hhvm

sudo: false
